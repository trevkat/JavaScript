function getAverage (a,b){
    
    
    var average = (a + b) / 2;
    console.log(average);
    return average
    
}

var myResult = getAverage(7,12);  //pass in numbers for a,b

console.log("the average is " + myResult);